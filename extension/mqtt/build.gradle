apply from: "$rootDir/bintray.gradle"

ext {
    description = 'An aloha extension module with mqtt'
    artifactId = 'aloha-mqtt'
}

jar {
    appendix = "mqtt"
}

dependencies {
    compile(
            "org.eclipse.paho:org.eclipse.paho.client.mqttv3:$mqttClientVersion"
    )
}