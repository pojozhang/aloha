dependencies {
    compileOnly(
            "org.springframework.boot:spring-boot-starter-data-redis:$springBootVersion"
    )
    testCompile(
            "org.springframework.boot:spring-boot-starter-data-redis:$springBootVersion"
    )
}

jar {
    appendix = "spring-data-redis"
}