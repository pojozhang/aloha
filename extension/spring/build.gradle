jar {
    appendix = "spring"
}

allprojects {
    dependencies {
        compileOnly(
                "org.springframework.boot:spring-boot-starter-web:$springBootVersion"
        )
    }
}

subprojects {
    dependencies {
        compile project(":extension:spring")
        testCompile project(":test:spring")
    }
}